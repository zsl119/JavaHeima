package com.zsl.constructor;
//任何类定义出来，默认都自带无参构造器
public class Train {
    String name;
    int price;
    //什么都不用写，本身自带无参数构造器
    //一旦定义一个有参数构造器，这个默认的无参数构造器就没有了
    //如果还需要无参数构造器则需要自己定义一个
}

